[versions]
# ---------------------------------------------------------
# Kotlin & Core
# ---------------------------------------------------------
kotlin = "2.4.20"
minekot-toolchain = "1.3.5"
detekt = "2.0.0-alpha.6"
junit = "4.13.2"
junit-platform = "6.1.3"
idea-version = "2026.1.5"
kotlinx-serialization = "1.11.0"
sigstore = "2.3.0"
binary-compatibility-validator = "0.18.2"

[libraries]
kotlin-test = { module = "org.jetbrains.kotlin:kotlin-test", version.ref = "kotlin" }
kotlin-compiler-embeddable = { module = "org.jetbrains.kotlin:kotlin-compiler-embeddable", version.ref = "kotlin" }
kotlin-compiler = { module = "org.jetbrains.kotlin:kotlin-compiler", version.ref = "kotlin" }
detekt-api = { module = "dev.detekt:detekt-api", version.ref = "detekt" }
detekt-test = { module = "dev.detekt:detekt-test", version.ref = "detekt" }
junit = { module = "junit:junit", version.ref = "junit" }
junit-vintage-engine = { module = "org.junit.vintage:junit-vintage-engine", version.ref = "junit-platform" }
kotlinx-serialization-json = { module = "org.jetbrains.kotlinx:kotlinx-serialization-json", version.ref = "kotlinx-serialization" }
sigstore-java = { module = "dev.sigstore:sigstore-java", version.ref = "sigstore" }

[plugins]
minekot-toolchain = { id = "org.minekot.toolchain", version.ref = "minekot-toolchain" }
kotlin-jvm = { id = "org.jetbrains.kotlin.jvm", version.ref = "kotlin" }
intellij-platform = { id = "org.jetbrains.intellij.platform" }
binary-compatibility-validator = { id = "org.jetbrains.kotlinx.binary-compatibility-validator", version.ref = "binary-compatibility-validator" }
